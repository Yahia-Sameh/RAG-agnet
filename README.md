# Simple Contract Assistant

## One-Minute Setup
```bash
# 1. Create venv
python3 -m venv venv
source venv/bin/activate

# 2. Install
pip install -r requirements.txt

# 3. Add API key to .env
nano .env  # Add your Gemini key

# 4. Run
python run.py
